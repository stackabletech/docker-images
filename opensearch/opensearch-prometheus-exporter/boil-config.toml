[versions."3.1.0.0".local-images]
# Build the plugin with the same Java version as OpenSearch itself.
java-devel = "21"

[versions."3.6.0.0".local-images]
# Build the plugin with the same Java version as OpenSearch itself.
java-devel = "25"

[versions."3.8.0.0".local-images]
# Build the plugin with the same Java version as OpenSearch itself.
java-devel = "25"
